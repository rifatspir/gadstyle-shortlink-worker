Stable full package: step05
Project: gadstyle-shortlink-worker
Package type: FULL REPLACEMENT ZIP
Source baseline: gadstyle-shortlink-worker(3).zip
Included patch chain: worker-patch-step01.zip, worker-patch-step02.zip, worker-patch-step03.zip, worker-patch-step04.zip, worker-patch-step05.zip
Status: MARKED STABLE

Exact files changed by included patches:
- src/index.ts
- PATCH_MANIFEST.txt

Included changes:
1. Companion manifests for app-only patch steps.
2. Added lightweight smart-download reporting support in the Worker.
3. Added smart_download_events D1 table creation/retention logic.
4. Added POST /api/download-events endpoint for app root smart redirect logging.
5. Extended GET /api/admin/stats to return smartDownloads data:
   - totalRequests
   - androidRedirects
   - iosRedirects
   - fallbackViews
   - sources
6. Added optional SMART_DOWNLOAD_LOG_SECRET validation when configured in Worker env.

Safety notes:
- Existing shortlink create/resolve/direct-resolve APIs remain unchanged.
- Existing recent click retention remains unchanged.
- No IP address or full user-agent is stored for smart-download reports.
- Smart-download event retention is capped to keep D1 lightweight.
