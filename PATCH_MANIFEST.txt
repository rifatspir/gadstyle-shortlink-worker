Patch: step08
Project: gadstyle-shortlink-worker
Baseline: gadstyle-shortlink-worker-step07-stable-full.zip
Type: patch-only

Changed files:
- src/index.ts
  - Cached schema initialization per Worker isolate to avoid running CREATE TABLE/INDEX checks on every request.
  - Made smart-download event logging lighter by inserting first and pruning retained rows only every 100 inserts instead of every single event.
  - Kept the same /api/download-events and /api/admin/stats API contract.
- PATCH_MANIFEST.txt
  - Updated for step08.

Unchanged behavior / safety:
- No shortlink route contract changes.
- No D1 table name changes.
- No admin stats response shape changes.
- No IP address or full user-agent storage added.
